import React from 'react';

const OAuthRedirect = () => {
  return (
    // This component needs to redirect to Auth0 tenant's `/authorize` endpoint
    <div>
      Redirecting...
    </div>
  );
};

export default OAuthRedirect;